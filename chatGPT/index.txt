## v3.7.6.3

Changes made
Fixed KaTeX readiness issue by adding a post-load renderer safety pass.
Added repair for JSON-overescaped LaTeX such as \\(...\\) and \\[...\\].
Kept currency-dollar protection before KaTeX rendering.
Replaced Mermaid mermaid.run() flow with direct mermaid.render() per diagram.
Mermaid no longer marks a diagram as completed before successful rendering.
Mermaid loading spinner now stays only while actually loading/rendering.
Added retry behavior for Mermaid library loading.
Added visible Mermaid failure message if the renderer does not load.
JavaScript syntax check passed.
No TikZJax.
Manual tests
Open the file fresh without navigating away first.
Go directly to the section with the Bayesian Mermaid loop.
Confirm the Mermaid spinner appears briefly.
Confirm the Mermaid diagram renders without needing to switch lessons.
Go directly to Example — PBGC Valuation.
Confirm \(...\) and \[...\] do not remain visible.
Confirm the PBGC equation renders as KaTeX.
Confirm dollar amounts render as dollar amounts, not raw \$.
Test a user lesson with JSON-overescaped math like \\(x^2\\).
Confirm regular math $x^2$, \(...\), and \[...\] still render.
Check browser console for Mermaid or KaTeX errors.

## v3.7.6.2

Changes made — v3.7.6.2
Added KaTeX auto-render retry when renderMathInElement is not ready yet.
Fixed delayed KaTeX loading that caused raw \(...\) and \[...\] to remain visible.
Added automatic protection for bare currency dollars before KaTeX runs.
Updated the Authoring Guide currency-dollar instructions.
Updated PBGC valuation and other dollar-value examples.
Added renderer loading feedback CSS:
spinner
loading message card
Added Mermaid loading feedback:
“Loading Mermaid renderer…”
“Rendering Mermaid diagram…”
Added Mermaid retry logic while the external library is still loading.
Added Mermaid failure message if the CDN/library does not load.
Removed remaining TikZJax residue.

Manual tests
Open Example — PBGC Valuation.
Confirm the sentence does not show raw \( or \[.
Confirm the display equation renders.
Confirm dollar amounts display as $24,000 and $159,400, not \$24,000.

Create a test section with bare currency:

<p>Benefit is $275,000 per year.</p>

Confirm it does not break KaTeX.

Create a test section with escaped currency:

<p>Benefit is \$275{,}000 per year.</p>

Confirm it displays correctly.

Confirm normal inline math still works:

$x^2 + y^2 = z^2$
Confirm \(...\) inline math still works.
Confirm \[...\] display math still works.
Open the Bayesian flowchart section.
Confirm a spinner/message appears while Mermaid loads/renders.
Confirm the Mermaid diagram eventually renders.
Confirm no silent blank area appears during Mermaid delay.
Re-test Plotly, JSXGraph, Arquero, Manim, and SVG sections.
Check browser console for serious errors.

## v3.7.6.1

Fixed:

Bare currency values like $24,000 no longer break KaTeX rendering.
Added automatic currency-dollar protection before KaTeX render.
Updated Authoring Guide with currency-dollar rules.
Updated PBGC valuation example and other dollar-value examples.
Removed remaining TikZJax residue.
JavaScript syntax check passed.

Test:

Open Example — PBGC Valuation.
Confirm the display equation renders.
Confirm \$24{,}000 and \$159{,}400 display as dollar amounts.
Test a new lesson section with a bare value like $275,000.
Confirm normal math like $x^2$ still renders.

## v3.7.6

Built on top of v3.7.5.

Included:

Export active built-in lesson JSON
Export editor lesson JSON
Export schema metadata
blockInventory
Legacy plots / tables cleanup
No empty plots: {} / tables: {} in exports
Legacy function registries exported as source strings
Import normalization for exported lessons
JavaScript syntax check passed
No TikZJax

Test on your end:

Export a built-in lesson as active lesson JSON.
Confirm JSON has schemaVersion, exportedFrom, sourceKind, sections, quiz, and blockInventory.
Confirm built-in plots/tables are represented under legacyRegistries, not empty plots / tables.
Import the exported built-in JSON.
Save it as a user lesson.
Preview it and confirm legacy plots/tables still render.
Export a user-created lesson with inline Plotly/Arquero blocks.
Confirm no empty plots: {} / tables: {} appear.
Confirm blockInventory lists inserted Plotly, Arquero, Mermaid, JSXGraph, Manim, or SVG blocks.
Do a full export → import → save → preview round trip.

## v3.7.5 - chatGPT

Built on top of v3.7.4.

Included:

Draggable lesson editor modal
Resizable lesson editor modal
Expanded/fullscreen editor mode
Reset editor-window button
Persistent editor size/position
Larger editing areas
Mobile-safe editor behavior
No TikZJax
JavaScript syntax check passed.

## v3.7.4 - chatGPT

Included:

Collapsible / hidden sidebar
Wide-content mode
Persistent layout settings
Responsive guardrails
No TikZJax
JavaScript syntax check passed.
